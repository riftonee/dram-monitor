# Secrets — never commit. All keys live in GitHub Secrets / local .env
.env
.env.*
# ...but the placeholder template IS committed (it holds no real secrets):
!.env.example
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# OS
.DS_Store
